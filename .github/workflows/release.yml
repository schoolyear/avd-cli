name: Release

on:
  push:
    tags:
      - 'v*'  # Trigger this workflow when a new tag is pushed that matches the pattern

jobs:
  build:
    name: Build and release Go binaries
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '^1.23'

      - name: Extract version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF##*/}" >> $GITHUB_ENV

      - name: Build the Go binary
        run: |
          mkdir -p bin
          GOOS=windows GOARCH=amd64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-windows-amd64.exe
          GOOS=windows GOARCH=arm64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-windows-arm64}.exe
          GOOS=darwin GOARCH=amd64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-macos-amd64
          GOOS=darwin GOARCH=arm64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-macos-arm64
          GOOS=linux GOARCH=amd64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-linux-amd64
          GOOS=linux GOARCH=arm64 go build -ldflags="-X 'main.Version=${{ env.VERSION }}'" -o bin/avdcli-linux-arm64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            bin/avdcli-windows-amd64
            bin/avdcli-windows-arm64
            bin/avdcli-darwin-amd64
            bin/avdcli-darwin-arm64
            bin/avdcli-linux-amd64
            bin/avdcli-linux-arm64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
